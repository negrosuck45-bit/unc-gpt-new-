# Repair Audit Notes

The Vercel project linked to the production repository is `unc-gpt-new` with project ID `prj_nhCMrgMoOFPJHeDRegvTGmqyN3lC` in team `team_7Srq2NO1vdOffP0zOaDr0Eta`. Its GitHub link is `negrosuck45-bit/unc-gpt-new-`, production branch `main`, framework `nextjs`, and domains include `https://unc-gptt.vercel.app` and `https://unc-gpt-new-opal.vercel.app`.

The existing Cloudflare gateway hostname in the source is `https://fragrant-band-d94a.blackmonkey098gg.workers.dev`. Passive probes confirmed the hostname accepts POST requests but returns `{"error":"Missing task or prompt"}` when neither field is present and `{"error":"Invalid task"}` for the tested `tts`, `voice`, `speech`, `audio`, `voice-chat`, `text-to-speech`, and `chat` task names. The repository’s existing gateway helper uses an authenticated `x-uncgpt-agent-secret` header and `/v1/run`, while the existing voice route used an unauthenticated ad-hoc POST at the base URL. The new voice adapter therefore keeps the existing gateway URL and secret header, sends both task/prompt and Aura metadata, accepts direct audio or common JSON audio envelopes, and preserves an explicit browser SpeechSynthesis fallback when the primary provider or playback fails.

## Final validation note

The repaired repository passes all 62 contract tests, focused ESLint, full TypeScript checking, and Next.js webpack compile mode. A complete `next build --webpack` still fails only while prerendering Next.js's special `/_global-error` route with `Cannot read properties of null (reading 'useContext')`, even when the custom global-error file is temporarily removed. This matches the reported Next.js 16 webpack framework issue documented in [Next.js discussion #94667](https://github.com/vercel/next.js/discussions/94667) and [issue #84994](https://github.com/vercel/next.js/issues/84994). The normal application routes compile successfully, and the local app rendered `/` at both 1440×900 and 390×844 without horizontal overflow.
